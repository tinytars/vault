node_modules/
npm-debug.log*

# `**/` matters: an agent's working directory can be at any depth. Agent memory lives only in
# plover-context, and an ephemeral worktree is a nested repository rather than content.
**/.claude/agent-memory/
**/.claude/worktrees/
